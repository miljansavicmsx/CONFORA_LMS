# Commands executed

- `git fetch origin`; verified tip `da5d8c197d1b2b20b526486cd06aef45b6e898a0`
- `gh api .../pulls/5` → merged
- Workflow YAML inspection (`ci.yml`, `accessibility.yml`, others)
- `docker create` broken argv → exit 125
- `docker create` quoted health-cmd → success; start; health=healthy
- `pg_isready`; `pg_available_extensions`; `CREATE EXTENSION vector`
- `docker image inspect pgvector/pgvector:pg16`
- Cleanup: `docker rm -f` temporary containers
- Evidence commit; push `governance/r0-7c1-database-service-analysis`
