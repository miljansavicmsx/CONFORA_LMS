# Pre-generation manifest inventory

See `pre_generation_manifest_inventory.json`.

- Tracked manifests: **13**
- Workspace membership from tracked `apps/*` + `packages/*` only
- `frontend-app`: tracked standalone, outside root workspace
- Untracked local manifests excluded from generation inputs
