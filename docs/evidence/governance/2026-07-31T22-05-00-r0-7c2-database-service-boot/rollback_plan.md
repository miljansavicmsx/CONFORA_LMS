# Rollback plan

1. Revert the two one-line quoting edits in:
   - `.github/workflows/ci.yml`
   - `.github/workflows/accessibility.yml`
2. Or revert the R0-7C2 merge commit on the integration branch.
3. Evidence folder may remain as historical record.

No schema, lockfile, or package rollback required.
