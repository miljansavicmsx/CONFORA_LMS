# Title alignment

Before Ready-for-Review, PR #8 title was:

`feat(frontend): learner complaint filing via submitLearnerComplaint (028D-2aS2)`

It was aligned via `gh pr edit` to the required exact title:

`feat(frontend): enable authenticated candidate complaint filing`

Then `gh pr ready 8` was executed.

Observed title at evidence closure matches the required title.
