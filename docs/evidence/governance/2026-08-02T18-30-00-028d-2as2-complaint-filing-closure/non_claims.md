# Non-claims

This slice does **not** claim:

- full `frontend-app` typecheck / production build green (pre-existing missing `vite-csp-preview.mjs` and unrelated modules such as staff `api-grievances` consumers remain incomplete);
- appeal filing, appeal listing, or `decisionRef` discovery;
- `GET /me/applications`;
- closure of TD-006;
- promotion of rejected D2/S2 branches as merge bases;
- restoration of the full rejected frontend graph;
- nest-auth-pilot sidebar / RBAC fan-out closure;
- anonymous public complaint filing UX (authenticated learner path retains identity).

Appeal status constants for this slice:

```text
APPEAL_UI = NOT_IMPLEMENTED
GET_ME_APPLICATIONS = NOT_IMPLEMENTED
APPEAL_OWNERSHIP_FIX = DEFERRED
TD_006 = OPEN
```
