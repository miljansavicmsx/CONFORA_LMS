# Non-claims

This corrective does **not** claim:

- full `frontend-app` typecheck / production Vite build green;
- promotion or import of `authStore.ts`;
- login-flow changes;
- closure of TD-006;
- appeal filing;
- restoration of rejected D2/S2 graphs;
- that legacy `auth-storage.ts` is removed (file may remain from prior
  promotion but is no longer used by the token provider).
