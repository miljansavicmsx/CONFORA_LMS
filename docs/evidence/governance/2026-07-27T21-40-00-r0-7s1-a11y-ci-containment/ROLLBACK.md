# Rollback

Revert the R0-7S1 commit(s) on `security/r0-7s1-a11y-ci-containment`, or
restore `.github/workflows/accessibility.yml` from parent `66356586`.

Do **not** force-push integration. Do **not** restore `contents: write` or
git push without a new owner decision.
