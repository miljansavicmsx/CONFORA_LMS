# F4 Validator Disposition

Absent tracked path:
scripts/ops/run-f4-8g-frontend-validation.mjs

F4_VALIDATOR = SEPARATE_FOCUSED_PREREQUISITE

A future prerequisite must identify the authoritative F4 contract, enumerate
every tracked helper and test, author or recover source only from approved
authority, and validate from a clean clone. It requires a separate owner scope,
bounded paths, focused tests, evidence-only closure, independent review, and
merge authorization.

Exit: the entrypoint and all inputs are tracked, declared validation runs from
a clean clone, and no production deployment or credential is used. R0-7E must
not absorb this repair silently.
