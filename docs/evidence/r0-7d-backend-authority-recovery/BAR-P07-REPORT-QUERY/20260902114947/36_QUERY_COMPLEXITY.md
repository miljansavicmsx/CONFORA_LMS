# Query Complexity

One dimension per method (status XOR schemeRef wrappers)
No HAVING / raw where / select / include / orderBy caller control
No export / generic DSL / Prisma aggregate()
CALLER_GROUPBY_DIMENSION_CONTROL=false
