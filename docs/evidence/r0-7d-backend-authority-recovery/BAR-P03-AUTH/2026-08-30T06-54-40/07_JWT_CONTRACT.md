TOKEN_SOURCE=Authorization Bearer header only
TOKEN_TYPE=ACCESS_TOKEN_ONLY
ALGORITHM=RS256_ONLY
ISSUER=exact OIDC_ISSUER_URL
AUDIENCE=exact OIDC_CLIENT_ID
SUBJECT=required sub
TENANT_CLAIM=required tenant_id UUID
EXP_REQUIRED=true
NBF_VALIDATED_WHEN_PRESENT=true
CLOCK_SKEW_SECONDS=30
EXTRACTOR=ExtractJwt.fromAuthHeaderAsBearerToken()
