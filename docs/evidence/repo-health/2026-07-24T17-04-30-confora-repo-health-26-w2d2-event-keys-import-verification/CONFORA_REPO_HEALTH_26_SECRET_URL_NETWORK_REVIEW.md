# CONFORA-REPO-HEALTH-26 — Secret / URL / Network Review

Scanned for secrets, JWT/token patterns, passwords as values, HTTP(S), localhost, fetch/axios/WebSocket/GraphQL, `process.env`.

| Category | Hits (blocking) |
|----------|-----------------|
| Secrets / credentials | **0** |
| URL / network | **0** |

Substring matches that are **not** secrets are classified in the false-positive document.
